@* @page
@model Labs.UI.Areas.Admin.Pages.CreateModel

@{
    ViewData["Title"] = "Create";
}
        
<h1>Create</h1>        
<h4>PetFood</h4>
<hr/>
<div class="row">
    <div class="col-md-4">
        <form method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <label asp-for="PetFood.Description" class="control-label">Описание</label>
                <input asp-for="PetFood.Description" class="form-control" />
                <span asp-validation-for="PetFood.Description" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="PetFood.Price" class="control-label">Стоимость (€)</label>
                <input asp-for="PetFood.Price" class="form-control" />
                <span asp-validation-for="PetFood.Price" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label asp-for="Image" class="control-label">Изображение</label>
                <input asp-for="Image" class="form-control" />
            </div>
            <div class="form-group mb-3">
                <label asp-for="PetFood.CategoryId" class="control-label">Категория</label>
                <select asp-for="PetFood.CategoryId" class ="form-control" asp-items="ViewBag.CategoryId"></select>
            </div>
            <div class="form-group mb-3">
                <label asp-for="PetFood.Name" class="control-label">Название</label>
                <input asp-for="PetFood.Name" class="form-control"/>
                <span asp-validation-for="PetFood.Name" class="text-danger"></span>
            </div>            
            <div class="form-group mb-3 d-flex justify-content-between">
                <input type="submit" value="Create" class="btn btn-primary"/>
                <a asp-page="Index" class="btn btn-secondary">Отменить</a>
            </div>
        </form>
    </div>
</div>

@section Scripts 
{
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
 *@

@page
@model Labs.UI.Areas.Admin.Pages.CreateModel

@{
    ViewData["Title"] = "Добавление товара";
}

<div class="container mt-4 mb-5">
    <div class="card shadow-sm mx-auto" style="max-width: 600px;">
        <div class="card-body">
            <h4 class="card-title mb-4">Добавление нового товара</h4>
            <form method="post" enctype="multipart/form-data">
                <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

                <div class="mb-3">
                    <label asp-for="PetFood.Name" class="form-label">Название</label>
                    <input asp-for="PetFood.Name" class="form-control" />
                    <span asp-validation-for="PetFood.Name" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="PetFood.Description" class="form-label">Описание</label>
                    <input asp-for="PetFood.Description" class="form-control" />
                    <span asp-validation-for="PetFood.Description" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="PetFood.Price" class="form-label">Цена (€)</label>
                    <input asp-for="PetFood.Price" class="form-control" />
                    <span asp-validation-for="PetFood.Price" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="PetFood.CategoryId" class="form-label">Категория</label>
                    <select asp-for="PetFood.CategoryId" class="form-select" asp-items="ViewBag.CategoryId"></select>
                </div>

                <div class="mb-3">
                    <label asp-for="Image" class="form-label">Изображение</label>
                    <input asp-for="Image" class="form-control" type="file" />
                </div>

                <div class="d-flex justify-content-between">
                    <input type="submit" value="Создать" class="btn btn-primary" />
                    <a asp-page="Index" class="btn btn-secondary">Отменить</a>
                </div>
            </form>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
